# README.md
# markdown-notes-viewer
A tiny offline Markdown editor + previewer.

## Usage
1. Open `index.html` in a browser.
2. Type Markdown in the text area.
3. The preview updates live.
4. Notes are saved in `localStorage` automatically.
